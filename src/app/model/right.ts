export class Right {
  id: number;
  rightName: string;
  rightDescription: string;

  constructor(id: number, rightName: string, rightDescription: string) {
    this.id = id;
    this.rightName = rightName;
    this.rightDescription = rightDescription;
  }
}
